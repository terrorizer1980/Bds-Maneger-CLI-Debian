#!/bin/bash

if [ -e /etc/init.d/bds ]
then
    echo "Estamos atualizando os serviços do bds-maneger"
    cp -f /opt/bds-maneger/start-on-system.sh /etc/init.d/bds
    systemctl daemon-reload
fi

echo "Ainda estamos montando uma pré-configuração"

exit 0